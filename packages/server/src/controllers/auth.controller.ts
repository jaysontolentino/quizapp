import { NextFunction, Request, Response } from 'express'

const signIn = function(req: Request, res: Response, next: NextFunction) {
    try {
        
    } catch (error) {
        next(error)
    }
}

const googleSignIn = function(req: Request, res: Response, next: NextFunction) {
    try {
        
    } catch (error) {
        next(error)
    }
}

const signUp = function(req: Request, res: Response, next: NextFunction) {
    try {
        
    } catch (error) {
        next(error)
    }
}

const signOut = function(req: Request, res: Response, next: NextFunction) {
    try {
        
    } catch (error) {
        next(error)
    }
}

const refreshToken = function(req: Request, res: Response, next: NextFunction) {
    try {
        
    } catch (error) {
        next(error)
    }
}

export default {
    signIn,
    googleSignIn,
    signUp,
    signOut,
    refreshToken
}